<%- include ('_header') -%>



<div class="profile">
    <div class="container mt-4 mb-4 p-3 d-flex justify-content-center">
        <div class="card p-4">
            <!-- <% if(data.length){ for(var i=0; i< data.length; i++) {%> -->
                <div class=" image d-flex flex-column justify-content-center align-items-center">
                    <button class="btn btn-secondary"> <img src="/images/up.png" height="100"
                            width="100"></button>
                    <span class="name mt-3">
                            <%= data[0].first_name %> <%= data[0].last_name %>
                    </span>
                    <span class="idd">
                        <%= data[0].email %>
                    </span>
                    <div class="d-flex flex-row justify-content-center align-items-center gap-2">
                        <span class="idd1">
                            <%= data[0].phone %>
                        </span> 
                    </div> <div class="d-flex flex-row justify-content-center align-items-center mt-3"> 
                        <div class=" d-flex mt-2"> 
                            <form action="/auth/bookings" method="POST">
                                <input type="submit" class="btn3 btn-dark" value="Bookings">
                            </form>
                            <!-- <button class="btn3 btn-dark">Bookings</button>  -->
                        </div>
                    </div> 
                    <div class=" d-flex mt-2"> <button class="btn1 btn-dark" id="edit profile"><a href="/updateprofile" >Edit Profile</button></a></div>
                    <div class=" d-flex mt-2"> <button class="btn2 btn-dark">View Wishlist</button> </div>
                    
                    
                </div>

                <!-- <% } }
                else
                { %>
                     <tr> 
                         <td colspan="3">No user</td> 
                     </tr> 
                 <% } 
                 %>  -->
        </div>
    </div>
    <%- include ('_footer') -%>